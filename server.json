{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.unibas-medfak/zammad-mcp-server",
  "title": "Zammad MCP Server",
  "description": "Production-ready Zammad MCP Server for Zammad 6.0+ and 7.x (tested on 7.1). Connect Claude, Cursor, and LLMs to your helpdesk via MCP.",
  "repository": {
    "url": "https://github.com/unibas-medfak/zammad-mcp-server",
    "source": "github"
  },
  "version": "0.0.1",
  "packages": [
    {
      "registryType": "oci",
      "registryBaseUrl": "https://ghcr.io",
      "identifier": "unibas-medfak/zammad-mcp-server",
      "version": "0.0.1",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [
        {
          "type": "named",
          "name": "--transport",
          "value": "stdio"
        }
      ],
      "environmentVariables": [
        {
          "name": "ZAMMAD_URL",
          "description": "Base URL of your Zammad instance.",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "ZAMMAD_HTTP_TOKEN",
          "description": "Zammad API token.",
          "isRequired": true,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "MCP_DENIED_TOOLS",
          "description": "Comma-separated list of disabled MCP tools, for example delete_ticket,delete_user,delete_organization.",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "ZAMMAD_MCP_AUTH",
          "description": "Client authentication mode for network transports: none (default) or static.",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "ZAMMAD_MCP_AUTH_TOKENS",
          "description": "Bearer tokens accepted over HTTP, as client_id:token pairs. Required when ZAMMAD_MCP_AUTH=static.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        }
      ]
    }
  ]
}
